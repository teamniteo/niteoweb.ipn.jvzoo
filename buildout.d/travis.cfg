[buildout]
extends = development.cfg

# disable extensions to speedup buildout
extensions -= buildout_versions

# disable some parts to speedup buildout
parts -=
    omelette
    instance

# we need to use latest dev version until a new release is made
auto-checkout =
    plone.api
